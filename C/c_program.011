
// (C program : Run-time division)

#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter the value of a = "); 
	scanf("%d",&a);
    printf("Enter the value of b = "); 
	scanf("%d",&b);
	printf("Division of the values = %d / %d = %d",a,b,a/b);
}
